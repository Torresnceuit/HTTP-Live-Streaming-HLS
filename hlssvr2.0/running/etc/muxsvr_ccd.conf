
<root>
	req_mq_conf = ../etc/mq_mcd/mq_ccd_2_mcd.conf
	rsp_mq_conf = ../etc/mq_mcd/mq_mcd_2_ccd.conf

	complete_so_file = ./cs_udp_complete.so
	complete_func_name = req_complete_func 

	bind_ip = 0.0.0.0
	bind_port = 80

	max_conn = 120000
	grid_num = 1000
	grid_distant = 100
	req_water_mark = 3000000
    send_ip_2_mq = 1     //0: not send ip_header to mq, 1: send ip_header to mq, ip_header 8 bytes
    send_event_2_mq = 0
    send_nearly_ok_threshold = 0
</root>
